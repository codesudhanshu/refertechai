// PLACEHOLDER DATA — REPLACE BEFORE LAUNCH
// Verify before this file ships: every `value` below. These are round
// placeholders, not counted figures. Publishing an unverified number is a
// factual claim about the business.
// Replace with real counts, or delete the StatsBand section from the pages
// that render it.

export interface StatItem {
  value: string;
  label: string;
}

export const stats: readonly StatItem[] = [
  { value: "—", label: "Projects delivered" },
  { value: "—", label: "Engineers on the team" },
  { value: "—", label: "Industries served" },
  { value: "—", label: "Countries worked in" },
];
